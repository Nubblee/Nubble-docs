Type:
  type: string
  enum: [daily-calendars]
  default: daily-calendars

Id:
  type: string
  description: 일별 캘린더 고유 식별자
  example: "group-123-daily-calendars-2025-05-01"

Identifier:
  type: object
  properties:
    type:
      $ref: '#/Type'
    id:
      $ref: '#/Id'
