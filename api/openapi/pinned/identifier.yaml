Identifier:
  type: object
  properties:
    type:
      $ref: '#/Type'
    id:
      $ref: '#/PinnedNoticeId'

Type:
  type: string
  enum: [pinned-notices]
  default: pinned-notices

PinnedNoticeId:
  type: string
  description: 고정된 공지사항의 고유 식별자
  example: "789"
