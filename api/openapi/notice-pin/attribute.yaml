PinnedAt:
  type: string
  format: date-time
  description: 공지사항 고정 설정 시간
  example: "2025-05-01T14:30:00+09:00"

Order:
  type: integer
  description: |
    고정 순서 (1부터 시작, 값이 작을수록 상단에 표시)
    지정하지 않으면 자동으로 가장 마지막 순서에 추가됩니다
  minimum: 1
  maximum: 3
  example: 1
