Identifier:
  type: object
  properties:
    type:
      $ref: '#/Type'
    id:
      $ref: '#/NoticePinId'

Type:
  type: string
  enum: [notice-pin]
  default: notice-pin

NoticePinId:
  type: string
  description: 고정된 공지사항의 고유 식별자
  example: "789"
